# 环境准备

后面课程的实验需要一套开发工具。本页给出清单、安装方式和验证方法。内容为编者建议；命令在 Ubuntu 24.04 上实际运行核对过。

## 需要的工具

| 用途 | 工具 |
|---|---|
| 版本管理 | Git |
| Python | Python 3，含 `venv` 模块 |
| C 与 C++ | `gcc`、`g++`、`make` |
| 调试与内存检查 | `gdb`、`valgrind` |
| 编辑器（可选） | [Visual Studio Code](https://code.visualstudio.com/) |

## 按系统安装

**Ubuntu / Debian**

```bash
sudo apt update
sudo apt install -y build-essential gdb valgrind git python3 python3-venv
```

`build-essential` 包含 `gcc`、`g++` 和 `make`。

**Windows**

- 推荐用 WSL2 安装 Ubuntu，再按上面的 Ubuntu 步骤安装：[安装 WSL（微软官方文档）](https://learn.microsoft.com/zh-cn/windows/wsl/install)。
- csdiy.wiki 也介绍了 Windows 上的包管理器：[Scoop](https://csdiy.wiki/必学工具/Scoop/)。

**macOS**

- 安装命令行工具：`xcode-select --install`，其中含 `git`、`clang` 和 `make`。
- 包管理器：[Homebrew](https://brew.sh/zh-cn/)。
- csdiy.wiki 推荐的配置指南：[macOS Setup Guide](https://sourabhbajaj.com/mac-setup/)。

## 验证

依次运行下面的命令，都能打印版本号就说明装好了（Ubuntu 24.04 上的输出如下，版本号可以不同）：

```text
$ git --version
git version 2.43.0
$ python3 --version
Python 3.12.3
$ gcc --version
gcc (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0
$ g++ --version
g++ (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0
$ make --version
GNU Make 4.3
$ gdb --version
GNU gdb (Ubuntu 15.1-1ubuntu1~24.04.1) 15.1
$ valgrind --version
valgrind-3.22.0
```

再跑通编译与虚拟环境：

```bash
# C 与 C++
printf '#include <stdio.h>\nint main(void){printf("hello, C\\n");return 0;}\n' > hello.c
gcc -Wall -Wextra hello.c -o hello && ./hello
printf '#include <iostream>\nint main(){std::cout<<"hello, C++\\n";}\n' > hello.cpp
g++ -Wall -Wextra hello.cpp -o hellopp && ./hellopp

# Python 虚拟环境
python3 -m venv .venv
source .venv/bin/activate
python -c "import sys; print(sys.prefix)"
deactivate
```

C 与 C++ 的两个程序应分别打印 `hello, C` 与 `hello, C++`；虚拟环境激活后，`sys.prefix` 应指向当前目录下的 `.venv`。

## 遇到问题

把完整的报错信息和你运行的命令贴给 AI，并写清系统版本，参考[学习方法](学习方法.md)里"解读报错"的做法。工具方面的更多内容见 [Missing Semester](https://missing.csail.mit.edu/)。
